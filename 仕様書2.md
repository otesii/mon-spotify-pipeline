# m-on.jp → Spotify プレイリスト自動更新パイプライン 改修仕様書 v2

## 前提

本仕様書は `mon-spotify-pipeline-spec.md`（v1）の改修版である。
v1で確定した以下の内容は変更なし：

- TSVダウンロード方式（`/oa/download/`）
- エンコーディング：CP932 → iconv-lite でUTF-8変換
- TSVパースロジック（番組名部分一致 → rowspan相当の連続行取得）
- Spotify Web API（OAuth 2.0 / Refresh Token方式）

---

## v2での変更点

| 項目 | v1 | v2 |
|---|---|---|
| 実行基盤 | GitHub Actions | **AWS Lambda** |
| トリガー | GitHub Actions cron | **EventBridge（定期） + API Gateway（LINE Bot）** |
| ログ | なし | **CloudWatch Logs** |
| インターフェース | なし | **LINE Bot（手動トリガー + 完了通知）** |

---

## システム全体構成

```
【定期実行】
EventBridge Scheduler（週次 or 複数回/週）
    ↓
Lambda（mon-spotify-pipeline）
    ↓
CloudWatch Logs（実行ログ）

【LINE Botトリガー】
LINEユーザー「あのころヒッツ作って」
    ↓
LINE Messaging API（Webhook）
    ↓
API Gateway（POST /webhook）
    ↓
Lambda（mon-spotify-pipeline）
    ↓
Spotify プレイリスト作成・更新
    ↓
Lambda → LINE返信「✅ 72曲のプレイリストを作成しました」
    ↓
CloudWatch Logs（実行ログ）
```

---

## AWS構成詳細

### Lambda

| 項目 | 値 |
|---|---|
| ランタイム | Node.js 20 |
| メモリ | 256MB（TSV取得＋Spotify API程度なら十分） |
| タイムアウト | 60秒（Spotify検索72回を考慮） |
| 環境変数 | Parameter Store or Lambda環境変数で管理 |
| ログ出力先 | CloudWatch Logs（自動連携） |

### EventBridge Scheduler

```
# 例：毎週月・水・金の朝9時（JST）に実行
cron(0 0 ? * MON,WED,FRI *)  ← UTC表記
```

- 番組の放送スケジュールに合わせて調整
- 手動実行（LINE Bot）と同じLambdaを呼び出す

### API Gateway

| 項目 | 値 |
|---|---|
| タイプ | HTTP API（REST APIより安価） |
| エンドポイント | POST /webhook |
| 認証 | LINE署名検証（X-Line-Signature）をLambda内で実施 |

### CloudWatch Logs

| 項目 | 設定値 | 理由 |
|---|---|---|
| ロググループ | `/aws/lambda/mon-spotify-pipeline` | Lambda自動作成 |
| 保持期間 | **30日** | デフォルト「失効しない」は要変更 |
| 無料枠 | 5GB/月 | 週数回・数十秒実行なら数MB/月程度 → 無料枠内に収まる |

**ログに出力する内容（構造化ログ推奨）：**
```json
{ "event": "fetch_tsv", "status": "ok", "rows": 283 }
{ "event": "parse_tracks", "program": "あのころヒッツ...", "count": 72 }
{ "event": "spotify_search", "title": "怪物", "artist": "YOASOBI", "status": "found", "track_id": "xxx" }
{ "event": "spotify_search", "title": "xxx", "status": "not_found" }
{ "event": "playlist_update", "status": "ok", "added": 70, "skipped": 2 }
{ "event": "line_reply", "status": "ok" }
```

---

## ディレクトリ構成

```
mon-spotify-pipeline/
├── src/
│   ├── index.js          # Lambdaハンドラ（イベント種別を振り分け）
│   ├── fetcher.js        # TSV取得＋パース
│   ├── spotify.js        # Spotify API操作
│   ├── line.js           # LINE Messaging API（署名検証・返信）
│   └── logger.js         # 構造化ログ出力ユーティリティ
├── .env.example
├── package.json
└── terraform/            # （任意）IaC管理する場合
    ├── main.tf
    ├── lambda.tf
    ├── eventbridge.tf
    ├── apigateway.tf
    └── cloudwatch.tf
```

---

## 各モジュール実装概要

### index.js（Lambdaハンドラ）

```js
export const handler = async (event) => {
  // イベント種別の判定
  if (event.source === 'aws.scheduler') {
    // EventBridgeからの定期実行
    await runPipeline();
  } else if (event.headers?.['x-line-signature']) {
    // API GatewayからのLINE Webhook
    await handleLineWebhook(event);
  }
};
```

### fetcher.js（変更なし・v1から流用）

```js
import axios from 'axios';
import iconv from 'iconv-lite';

export async function fetchTracks(targetProgram) {
  const res = await axios.get('https://www.m-on.jp/oa/download/', {
    responseType: 'arraybuffer'
  });
  const text = iconv.decode(Buffer.from(res.data), 'cp932');
  const lines = text.split(/\r?\n/);
  // ... パースロジック（v1参照）
}
```

### line.js

```js
// 1. X-Line-Signatureの検証（Channel Secretで署名確認）
// 2. メッセージテキストから番組名を抽出（例：「あのころヒッツ作って」）
// 3. パイプライン実行後、結果をreplyTokenで返信
//    成功: 「✅ あのころヒッツ! 一挙5時間！ 2021→2025年\n72曲のプレイリストを更新しました」
//    失敗: 「❌ エラーが発生しました: {message}」
```

### logger.js

```js
// CloudWatch Logsに構造化JSONを出力するユーティリティ
export const log = (event, data) => {
  console.log(JSON.stringify({ event, timestamp: new Date().toISOString(), ...data }));
};
```

---

## 環境変数

```env
# Spotify API
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REFRESH_TOKEN=
SPOTIFY_PLAYLIST_ID=

# 対象番組名（部分一致）
TARGET_PROGRAM_NAME=あのころヒッツ! 一挙5時間！ 2021→2025年

# LINE Messaging API
LINE_CHANNEL_SECRET=
LINE_CHANNEL_ACCESS_TOKEN=
```

Lambda環境変数 or AWS Systems Manager Parameter Store（SecureString）で管理。
機密情報（トークン類）は**Parameter Store推奨**。

---

## LINE Bot セットアップ手順

1. [LINE Developers](https://developers.line.biz/) でチャンネル作成（Messaging API）
2. `Channel Secret` と `Channel Access Token` を取得
3. Webhook URL に API Gateway エンドポイントを設定
   `https://{api-id}.execute-api.ap-northeast-1.amazonaws.com/webhook`
4. 「Webhookの利用」をONにする

---

## IAMロール（Lambda実行ロール）に必要な権限

```json
{
  "Effect": "Allow",
  "Action": [
    "logs:CreateLogGroup",
    "logs:CreateLogStream",
    "logs:PutLogEvents"
  ],
  "Resource": "arn:aws:logs:*:*:*"
}
```

Parameter Storeを使う場合は追加で：
```json
{
  "Effect": "Allow",
  "Action": ["ssm:GetParameter", "ssm:GetParameters"],
  "Resource": "arn:aws:ssm:ap-northeast-1:{account-id}:parameter/mon-spotify/*"
}
```

---

## CloudWatch Logs 料金メモ

| 項目 | 無料枠 | 本パイプラインの想定使用量 |
|---|---|---|
| データ取り込み | 5GB/月 | 数MB/月（週数回・数十秒） |
| アーカイブ | 5GB/月 | 同上 |
| Logs Insightsクエリ | 5GB/月スキャン | デバッグ時のみ使用 |
| **合計** | **無料枠内** | **$0** |

> ⚠️ 保持期間のデフォルトは「失効しない」なので、**必ず30日等に設定すること。**

---

## 実装ステータス

| タスク | 状態 |
|---|---|
| v1仕様策定（TSV・Spotify） | ✅ 完了 |
| TSV動作確認（72曲・CP932） | ✅ 完了 |
| Spotify Developer App 作成 | ⬜ 未着手 |
| Refresh Token 取得 | ⬜ 未着手 |
| Lambda関数作成（基本構成） | ⬜ 未着手 |
| fetcher.js 実装 | ⬜ 未着手 |
| spotify.js 実装 | ⬜ 未着手 |
| CloudWatch Logs 保持期間設定 | ⬜ 未着手 |
| EventBridge Scheduler設定 | ⬜ 未着手 |
| LINE Bot チャンネル作成 | ⬜ 未着手 |
| API Gateway設定 | ⬜ 未着手 |
| line.js 実装（署名検証・返信） | ⬜ 未着手 |
| index.js イベント振り分け実装 | ⬜ 未着手 |
| 結合テスト（定期実行） | ⬜ 未着手 |
| 結合テスト（LINE Botトリガー） | ⬜ 未着手 |

---

## 推奨実装順序

1. **fetcher.js + spotify.js** をローカルで動作確認（v1相当）
2. **Lambda化**（index.js のハンドラ化、環境変数対応）
3. **EventBridge** で定期実行を設定・動作確認
4. **CloudWatch Logs** で構造化ログを確認（Logs Insightsでクエリ練習）
5. **LINE Bot + API Gateway** を追加（最後に統合）